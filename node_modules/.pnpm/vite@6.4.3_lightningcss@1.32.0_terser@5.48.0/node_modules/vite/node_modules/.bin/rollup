#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../rollup/dist/bin/rollup" "$@"
else
  exec node  "$basedir/../../../rollup/dist/bin/rollup" "$@"
fi
# cmd-shim-target=C:/Users/saade/Documents/SoriKyo/Sorikyo-warehouse/node_modules/.pnpm/vite@6.4.3_lightningcss@1.32.0_terser@5.48.0/node_modules/rollup/dist/bin/rollup
